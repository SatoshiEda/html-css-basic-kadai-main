% git config --local user.name "衣田敏"
% git config --local user.email "engineer.mentor.se@gmail.com"

% git init
% git add .
% git commit -m "コミットコメント"

% git push -u origin main

現在のブランチを確認
% git status



% git checkout main
% git pull

% git checkout -b pull_request_branch
% git add .
% git commit -m "プルリクエストのテスト" 

GitHubで「pull_request_branch」ブランチ作成

% git push origin pull_request_branch